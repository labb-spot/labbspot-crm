{
  "name": "labbspot-crm",
  "version": "1.0.0",
  "private": true,
  "description": "LABB Spot — real estate underwriting + workflow tool. Tailwind is precompiled at build time (no runtime JIT) for performance.",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
